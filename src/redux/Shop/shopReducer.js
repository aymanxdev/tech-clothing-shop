import shopData from "../../pages/shop/collections.data";
const initialState = {
  shopData,
};

export const shopReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
